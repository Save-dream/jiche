# 二手摩托车获客小程序分期开发规划文档

**文档版本**：V1.3 分期版（2026-07-08 对齐实现）  
**技术栈**：Django 4.2 + Vue + MySQL 8.0+  
**目标**：一期可内测跑通业务闭环，二期可商家自运营，三期可平台级管控

---

## 项目概述与分期原则

| 分期 | 目标 | 核心原则 |
|---|---|---|
| **一期** | **业务闭环 MVP（Web 先行）** | **Web C 端 + 商家后台 + 管理后台**跑通闭环；分享短链签名纳入一期；搜索筛选可提前交付。微信小程序客户端顺延二期。 |
| **二期** | **体验增强与商家赋能** | 补齐微信小程序端、消息强提醒、数据看板、媒体压缩规范、登录 Token 进阶策略。让商家愿意持续使用。 |
| **三期** | **平台化与风控** | 平台级数据看板、防刷反垃圾、内容安全、SEO、批量操作、高级推送。为规模化做准备。 |

**分期边界说明**：  
- 一期以 **Vue Web** 完成内测闭环；小程序端延后至二期，后端预留小程序登录 API。  
- 一期**已纳入**分享 HMAC 签名 + 短链；不做订阅消息（仅站内红点）、不做图片自动压缩。  
- 商家域搜索/筛选已提前落地，不再作为二期阻塞项。  
- 三期引入运营和风控工具，不新增核心业务流程。

---

## 一期：核心闭环（MVP）

### 一期功能大纲

1. **多端微信登录与账号体系**（一期：Web 扫码登录；后端预留小程序登录；unionid 统一账号）
2. **多租户隔离与分享链路**（`shop_id` 强制校验 + HMAC 签名 + 短链 + 最近访问商家）
3. **商家入驻与管理员审核**（商家名称必填 → 审核通过/驳回 → 解锁发布权限）
4. **机车发布/编辑/下架/已售/删除**（丰富车况字段 + published_at 排序 + 逻辑删除）
5. **C 端商家域浏览**（商品详情页 + 商家主页 + 在售/已售/全部 Tab + 搜索筛选 + 二维码展示）
6. **收藏与删除**（添加收藏、删除收藏、失效态展示）
7. **多轮留言咨询（基础版）**（会话创建、消息收发、未读红点聚合接口、进入自动已读）
8. **商家后台**（车源 CRUD、标记已售、分享短链、留言回复、资料/二维码管理）
9. **管理后台**（商家审核、商户封禁、全平台车源管控、违规下架、管理员授权）
10. **基础数据表与索引**（含 `share_link`）

---

### 1. 多端微信登录与账号体系

#### 1.1 小程序登录
- 启动时调用 `wx.login` 获取 code，后端调用微信 `code2session` 换取 `openid`，优先获取 `unionid`
- 若 `unionid` 获取失败（未绑定开放平台），降级使用 `openid` 匹配，但需在用户资料页提示"绑定微信可多端同步"
- 首次登录自动创建 `user` 记录，字段：`mp_openid`、`unionid`、`nickname`、`avatar_url`
- 登录成功后返回 `access_token`（小程序端有效期 30 天，静默续期）
- 未登录用户访问需登录页面时，拦截并引导授权（小程序内直接调起登录，无独立登录页）

#### 1.2 Web 扫码登录
- 统一登录入口 `/login`，页面展示微信扫码二维码
- 二维码内容：携带 `ticket`（UUID 生成，有效期 5 分钟）
- 用户微信扫码后，进入小程序确认页或微信开放平台扫码页，后端将 `ticket` 与 `user` 绑定
- Web 端每 3 秒轮询 `/auth/wx-scan/poll` 查询 ticket 状态
- 状态流转：`0 等待扫码` → `1 已扫码待确认` → `2 登录成功` → `3 已过期`
- 登录成功后返回 `access_token`（Web 端有效期 7 天）+ `refresh_token`（30 天）
- 同一用户最多 5 个活跃 token，超出时踢掉最早登录的 token
- 登录成功后跳转 `redirect` 参数指定的原页面

#### 1.3 多端数据共享
- 小程序与 Web 通过 `unionid`（优先）或 `openid` 映射到同一条 `user` 记录，统一 `user_id`
- 收藏、咨询会话、最近访问商家、入驻状态等均以 `user_id` 存储，任意端登录后数据一致
- 各端独立持有 token，可同时在线；登出仅清除当前端 token

#### 1.4 未登录访问策略
- **无需登录可访问**：首页引导页、商品详情（分享链进入）、商家主页（分享链进入）
- **需登录可访问**：我的收藏、我的咨询、个人中心、商家入驻、留言发送、收藏操作
- **需登录+权限**：商家后台（`shop_status=2`）、管理后台（`is_staff=1`）

---

### 2. 多租户隔离与分享链路

#### 2.1 分享链接类型
- **商品分享短链**：商家后台生成，格式如 `https://domain.com/s/{short_code}`，解析后进入商品详情
- **商品签名长链**：`/bike/{bike_id}?shop_id=xxx&timestamp=xxx&sign=xxx`
- **店铺分享链接**：path 仅携带 `shop_id`，直接进入商家店铺页
- 链接均不可跨租户访问其他商家数据

#### 2.2 分享安全与短链（一期必做）
- 签名规则：`sign = HMACSHA256(shop_id + "_" + bike_id + "_" + timestamp, SECRET_KEY)`
- 签名有效期 **7 天**；验签失败或过期返回 `403`，提示联系商家获取最新链接
- 短链表 `share_link`：`short_code`（6 位）、`shop_id`、`bike_id`、`timestamp`、`sign`、`expired_at`、`click_count`
- 商家点击「复制分享链接」时后端生成短链并返回；前端通过 `/s/:code` 解析跳转
- 站内同商家域跳转：仅需合法 `shop_id`（归属校验）；若携带 `timestamp/sign` 则必须验签通过

#### 2.3 后端校验规则
- C 端请求商品详情、商家主页时，必须携带合法 `shop_id` 参数
- 后端校验 `bike_id` 是否属于该 `shop_id`，若不匹配返回 `403`
- 商家后台所有 API 自动从当前登录用户的 `shop_id` 中提取，禁止传入其他 `shop_id`
- 管理员后台跳过 `shop_id` 校验，返回全平台数据

#### 2.4 最近访问商家
- 用户通过分享链接进入商家主页或商品详情时，后端记录 `user_shop_visit`
- 首页展示「最近访问商家」列表，取最近 **10 条**，按访问时间倒序，用于撑满首屏
- 无分享链接进入首页时，展示引导文案 + 快捷入口 + 最近访问商家（如有）

#### 2.5 首页引导页
- 未携带分享参数进入时，展示品牌引导文案
- 提供「我的收藏」「我的咨询」快捷入口（需登录）
- 无全平台车源广场、无跨商家列表

---

### 3. 商家入驻与管理员审核

#### 3.1 入驻申请
- 入口：个人中心 → 商家入驻（仅 `shop_status=0` / `3` 可见）
- 字段：**商家名称（必填）**、入驻类型、联系人姓名、联系电话、经营地址、主营车型、入驻说明、营业执照/资质图片（企业必填）、微信二维码（必填）
- 审核通过后，Shop 名称直接使用用户填写的商家名称
- 提交后 `shop_status` 变为 `1`（待审核），不可重复提交

#### 3.2 管理员审核
- 管理后台「商家审核」列表展示：申请时间、商家名称、联系人、电话、状态
- 操作：通过（`shop_status=2`）/ 驳回（`shop_status=3`，需填写驳回理由）
- 审核通过后，该用户解锁商家后台权限，可发布车源

#### 3.3 入驻状态流转
- `0` 普通用户 → `1` 待审核 → `2` 审核通过 / `3` 审核驳回
- `3` 审核驳回后，用户可修改资料重新提交，回到 `1`
- `4` 违规封禁仅管理员可操作，本期仅实现封禁按钮，不实现申诉

---

### 4. 机车发布/编辑/下架/删除

#### 4.1 车源字段（丰富版，与实现对齐）
- 基础：品牌、车型、排量、年份、行驶里程、价格、是否可过户、是否可议价、过户次数、交付方式等
- 车况分项：发动机/减震/刹车/电控/车架状态、改装明细、瑕疵说明、维保记录、费用说明、售后说明
- 图片：展示图最多 **20 张**，车况实拍图最多 **10 张**；单张 ≤5MB，格式 JPG/PNG；封面取首图
- 发布时自动记录 `published_at`（首次上架时间），编辑时不改变 `published_at`

#### 4.2 车辆状态与操作
- `bike_status=1` 在售：C 端可展示，商家可手动下架或标记已售
- `bike_status=2` 已售：商家标记已售，C 端展示已售标签（仍可见）
- `bike_status=3` 下架：商家手动下架，**C 端不可见**，商家后台可见，可自行重新上架
- `bike_status=4` 违规下架：管理员强制下架，**本期商家不可恢复**，仅展示「联系平台」；C 端不可见
- 商家删除：逻辑删除 `is_deleted=1`，C 端不可见，收藏列表展示「商家已删除」

#### 4.3 列表排序与可见性
1. **C 端仅展示在售 + 已售**；下架 / 违规下架不在用户侧展示
2. 优先展示 `bike_status=1`（在售）
3. 同状态内按 `published_at` 倒序
4. `bike_status=2`（已售）排在在售之后，同样按 `published_at` 倒序
5. Tab 筛选：在售（默认）/ 已售 / 全部（C 端全部=在售+已售；商家后台另含「已下架」）
6. 商家主页支持关键词搜索 + 价格/排量/年份等筛选（一期提前交付）

---

### 5. C 端商家域浏览

#### 5.1 商品详情页
- 仅通过分享链接或从同商家域内跳转进入
- 展示：车辆图片轮播、基础信息（品牌/车型/排量/年份/里程/价格/过户）、车辆描述、商家信息卡片（名称+地址+微信二维码）
- 底部固定操作栏：收藏、留言咨询、分享（调用小程序原生分享，携带当前 `shop_id` + `bike_id`）
- 微信二维码区域：提示「长按识别二维码，添加商家微信咨询」

#### 5.2 商家主页
- 展示商家名称、地址、微信二维码、在售/已售车源列表
- 默认展示「在售」Tab，可切换「已售」「全部」Tab
- 列表排序与可见性同 4.3 规则
- 支持关键词搜索与多维度筛选（一期提前交付，此前二期项）

#### 5.3 未登录访问
- 商品详情和商家主页允许未登录浏览，但收藏和留言需登录后操作
- 未登录用户点击收藏/留言时，引导登录

---

### 6. 收藏与删除

#### 6.1 添加收藏
- 商品详情页点击收藏，写入 `favorite` 表（`user_id` + `bike_id` + `created_at`）
- 收藏成功后按钮变为「已收藏」状态

#### 6.2 我的收藏列表
- 入口：个人中心 → 我的收藏
- 列表展示：车辆封面图、标题、价格、收藏时间
- 商品被商家逻辑删除（`is_deleted=1`）后，列表展示「商家已删除」灰色标签，**禁用点击进入详情**
- 支持删除收藏：左滑删除（小程序）或点击删除（Web），物理删除 `favorite` 记录

---

### 7. 多轮留言咨询（基础版）

#### 7.1 会话创建
- 入口：商品详情页「留言咨询」→ 进入会话页；如该用户与该商品无会话，自动创建 `message_thread`（`user_id` + `shop_id` + `bike_id`）
- 一个用户 + 一个商品 = 一个会话，不可重复创建

#### 7.2 消息收发
- 会话页类微信对话布局：用户消息右气泡，商家消息左气泡
- 单条消息内容纯文本，≤500 字
- 首条消息可携带联系电话（选填，输入框下方勾选「留下我的电话」），后续消息仅文本
- 消息表 `message_item` 字段：`thread_id`、`sender_type`（1用户/2商家）、`content`、`contact_phone`（首条可选）、`is_read`、`created_at`

#### 7.3 未读与红点
- 进入会话页自动将该会话的对方消息标记为已读
- TabBar / 导航角标展示未读数（后端聚合接口 `GET /messages/unread-count?role=user|shop`）
- 商家后台「留言管理」菜单展示未读消息角标
- 本期**不做微信订阅消息推送**，仅做站内红点提醒
- 前端轮询未读数：每 30 秒查询一次 `/messages/unread-count`

#### 7.4 权限隔离
- 用户仅可见自己的会话列表和会话详情
- 商家仅可见本店会话列表和会话详情
- 管理员后台可全平台只读查看，不可代回复

---

### 8. 商家后台

#### 8.1 车源管理
- 车源列表：默认在售优先 + `published_at` 倒序；Tab 筛选在售/已售/全部/已下架
- 操作：新增、编辑、标记已售、下架、重新上架（仅 `status=3`）、删除（逻辑删除）
- `status=4`（违规下架）仅展示状态，无「重新上架」按钮，提示「联系平台处理」
- 列表支持一键复制分享短链（后端生成 HMAC 签名短链，7 天有效）

#### 8.2 留言管理
- 会话列表：展示用户昵称、最后一条消息摘要、最后消息时间、未读红点
- 点击进入会话详情，可回复消息；回复后该会话对该商家标记为已读
- 本期**不做归档功能**，会话永久展示在列表中

#### 8.3 商家资料
- 编辑商家名称、联系人、电话、地址、微信二维码图片
- 查看入驻状态

---

### 9. 管理后台

#### 9.1 商家审核
- 待审核列表：展示申请信息、操作通过/驳回
- 驳回需填写理由，用户端可见

#### 9.2 商户管理
- 全平台商家列表：展示商家名称、联系人、状态、入驻时间
- 操作：封禁（`shop_status=4`）、解封（回到 `2`）

#### 9.3 车源管控
- 全平台车源列表：可查看任意商家车源
- 操作：违规下架（`bike_status=4`，需填写下架理由）
- 违规下架后，**仅管理员可恢复为在售**（`bike_status=1`）

#### 9.4 留言查看
- 全平台会话列表，只读，不可回复
- 支持按商家、用户、时间筛选

#### 9.5 管理员管理
- 平台用户列表，可按昵称/手机号搜索
- 操作：授予 `is_staff=1`（设为管理员）、撤销 `is_staff=0`
- **首个预置超级管理员不可被撤销**（通过 `is_superuser` 或预设 ID 保护）
- 管理员自身不可撤销自己的 `is_staff`

---

### 10. 一期数据库核心表

| 表名 | 核心字段 | 索引 |
|---|---|---|
| `user` | `id`, `mp_openid`, `web_openid`, `unionid`, `nickname`, `avatar_url`, `is_staff`, `created_at` | `unionid`（唯一）, `mp_openid`（唯一） |
| `auth_login_ticket` | `id`, `ticket`, `user_id`, `status`, `expired_at`, `created_at` | `ticket`（唯一） |
| `shop` | `id`, `user_id`, `name`, `contact_name`, `phone`, `address`, `wx_qr_url`, `status`, `created_at` | `user_id`（唯一）, `status` |
| `shop_application` | `id`, `user_id`, `name`（商家名称）, `contact_name`, `phone`, `address`, `license_url`, `wx_qr_url`, `status`, `reject_reason`, `created_at` | `user_id`, `status` |
| `bike` | `id`, `shop_id`, 基础与车况字段, `cover_image`, `status`, `published_at`, `is_deleted`, `created_at`；媒体走 `bike_media` | `shop_id` + `status` + `published_at`, `shop_id` + `is_deleted` |
| `bike_media` | `id`, `bike_id`, `shop_id`, `media_type`, `url`, `sort_order` | `bike` + `media_type` + `sort_order` |
| `share_link` | `id`, `short_code`, `shop_id`, `bike_id`, `timestamp`, `sign`, `expired_at`, `click_count`, `created_at` | `short_code`（唯一）, `shop` + `bike`, `expired_at` |
| `message_thread` | `id`, `user_id`, `shop_id`, `bike_id`, `status`, `last_message_at`, `created_at` | `user_id` + `last_message_at`, `shop_id` + `last_message_at` |
| `message_item` | `id`, `thread_id`, `sender_type`, `content`, `contact_phone`, `is_read`, `created_at` | `thread_id` + `created_at` |
| `favorite` | `id`, `user_id`, `bike_id`, `created_at` | `user_id` + `created_at`, `user_id` + `bike_id`（唯一） |
| `user_shop_visit` | `id`, `user_id`, `shop_id`, `visit_time`, `created_at` | `user_id` + `visit_time` |

---

## 二期：体验增强与商家赋能

### 二期功能大纲

1. **微信小程序客户端**（静默登录 + 原生分享 + 与 Web 能力对齐）
2. **搜索历史**（关键词本地缓存；主搜索/筛选已在一期交付）
3. **消息提醒强化**（微信订阅消息授权引导 + 未授权兜底 + 站内强提醒）
4. **会话归档**（用户/商家独立归档 + 新消息自动取消归档）
5. **违规申诉流程**（商家提交申诉 → 管理员处理 → 状态流转）
6. **商家数据看板**（今日 PV/咨询/收藏/分享 + 7 日趋势 + TOP5 车源）
7. **埋点体系**（核心事件定义 + 上报机制 + event_log 表）
8. **媒体规范增强**（自动压缩 + 缩略图；图片上限以一期丰富版为准）
9. **登录 Token 进阶**（小程序 30 天 access、活跃 token 上限踢人、refresh 续期）
10. **商家活跃度标记**（最后登录/最后发布记录，低活跃标记）

> 注：分享短链签名、商家域搜索筛选、Web C 端主体已前移至一期。

---

### 1. 微信小程序客户端

- 实现小程序端页面与 Web C 端能力对齐
- `wx.login` 静默登录对接 `/auth/wx-mini/login/`
- 原生分享 `onShareAppMessage` 携带短链或签名参数
- TabBar 未读角标与站内红点

---

### 2. 搜索历史（补齐）

- 缓存用户最近 5 条搜索关键词（小程序用 `wx.setStorage`，Web 用 `localStorage`）
- 搜索栏聚焦时展示历史记录，可一键清除

---

### 3. 消息提醒强化

#### 3.1 订阅消息授权引导
- 商品详情页「留言咨询」按钮下方、会话页输入框上方，增加强提示文案：「开启消息提醒，商家回复后第一时间通知您」
- 用户未授权时，点击「留言咨询」先弹出授权引导浮层，用户可选择「允许提醒」或「暂不开启」
- 授权后调用 `wx.requestSubscribeMessage` 订阅「商家回复通知」模板

#### 3.2 订阅消息推送
- 商家回复用户后，如用户已授权订阅消息，后端调用微信模板消息接口推送
- 模板内容：商家名称、回复内容摘要（前 20 字）、回复时间
- 点击模板消息跳转小程序对应会话页

#### 3.3 未授权兜底方案
- 用户未开启订阅消息时，商家回复后，用户下次进入小程序（启动或切前台）时：
  - TabBar「我的」红点强化
  - 首页顶部 Banner 强提示：「您有 X 条新回复，点击查看」
- 兜底提示持续展示直到用户进入会话页标记已读

---

### 4. 会话归档

#### 4.1 归档操作
- 会话列表页支持左滑（小程序）或点击更多（Web）操作「归档」
- `message_thread` 表新增字段：`archived_by_user`（0/1）、`archived_by_shop`（0/1）
- 用户归档后，该会话从用户端的「我的咨询」列表移除；商家归档后从商家后台移除
- 归档操作仅影响当前视角，不影响对方

#### 4.2 自动取消归档
- 归档会话有新消息进入时，自动将对应角色的归档标记重置为 0，该会话重新出现在列表顶部
- 未读计数不受归档影响，新消息仍计入未读

---

### 5. 违规申诉流程

#### 5.1 商家提交申诉
- 商家后台「车源管理」列表中，`bike_status=4` 的车辆展示「申诉」按钮
- 申诉表单：申诉理由（文本 ≤500 字）、补充图片（≤3 张）
- 提交后写入 `bike_appeal` 表，`status=1`（申诉中）

#### 5.2 管理员处理
- 管理后台「商家审核」页增加「申诉处理」Tab
- 列表展示：商家名称、违规车源、违规原因、申诉时间、申诉状态
- 操作：查看申诉详情（理由 + 图片）→ 驳回申诉（`status=2`，需填写驳回理由）/ 撤销违规（`status=3`，车辆恢复为 `bike_status=1`）
- 商家后台可查看申诉进度和驳回理由

---

### 6. 商家数据看板

#### 6.1 数据概览卡片
- 商家后台首页（或独立「数据中心」页）展示：
  - 今日浏览量（PV）：该车商所有车源今日被浏览次数（基于埋点 `bike_view` 聚合）
  - 今日咨询量：今日新发起会话数
  - 今日收藏数：今日新增收藏数
  - 今日分享次数：今日分享链接被复制/生成次数

#### 6.2 趋势图
- 近 7 日浏览/咨询/收藏曲线折线图（简单图表，前端用 ECharts 或 Chart.js）
- 数据每日凌晨由后端聚合写入看板缓存表，避免实时统计压力

#### 6.3 车源表现排行
- 按近 7 日浏览量倒序展示 TOP 5 车源
- 提示文案：「该车源较受欢迎，建议重点推广」
- 点击跳转该车源编辑页

---

### 7. 埋点体系

#### 7.1 核心事件定义

| 事件名 | 触发时机 | 携带参数 |
|---|---|---|
| `share_link_generate` | 商家点击生成/复制分享链接 | `shop_id`, `bike_id`, `source_page` |
| `share_link_open` | 用户打开分享链接（短链解析成功） | `shop_id`, `bike_id`, `utm_source` |
| `bike_view` | 用户浏览商品详情页超过 3 秒 | `shop_id`, `bike_id`, `duration_ms` |
| `consult_start` | 用户点击「留言咨询」并成功创建会话 | `shop_id`, `bike_id`, `thread_id` |
| `message_send` | 用户/商家发送消息 | `shop_id`, `thread_id`, `sender_type` |
| `favorite_add` | 用户点击收藏 | `shop_id`, `bike_id` |
| `favorite_remove` | 用户取消收藏 | `shop_id`, `bike_id` |
| `wx_qr_click` | 用户长按/点击商家微信二维码 | `shop_id` |

#### 7.2 上报机制
- 小程序使用 `wx.request` 异步上报至 `POST /events`，不阻塞主流程
- Web 使用 `navigator.sendBeacon`（页面关闭时）或 `fetch`（正常操作时）
- 上报失败时本地缓存（`wx.setStorage` / `localStorage`），下次启动补报

#### 7.3 数据表
- `event_log`：`id`, `event_name`, `user_id`, `shop_id`, `bike_id`, `params`（JSON）, `created_at`
- 索引：`event_name` + `created_at`, `user_id` + `created_at`

---

### 8. 媒体规范增强

#### 8.1 上传限制（与一期实现一致）
- 展示图最多 **20 张**，车况实拍图最多 **10 张**
- 单张图片格式：JPG/PNG，≤ 5MB

#### 8.2 自动压缩（二期补齐）
- 后端接收图片后，使用 Pillow 自动压缩：宽度 1080px，质量 80%，目标体积 ≤ 500KB
- 压缩后存储至 OSS/对象存储，返回 CDN URL

#### 8.3 缩略图
- 自动生成 300x300 缩略图，用于列表展示
- `cover_image` 已在一期存在；二期补充缩略图衍生策略

---

### 9. 登录 Token 进阶

- 小程序 access_token 有效期 30 天，Web 端 7 天 + refresh 30 天
- 同一用户最多 5 个活跃 token，超出踢掉最早登录
- 前端接入 refresh 续期流程

---

### 10. 商家活跃度标记

#### 10.1 记录维度
- `shop` 表新增字段：`last_login_at`（商家后台最后登录时间）、`last_publish_at`（最后发布时间）
- 商家每次登录后台或发布/编辑车源时，后端自动更新对应时间戳

#### 10.2 低活跃标记
- 180 天未登录且无非下架车辆（`bike_status=1` 数量为 0）时，`shop` 标记 `is_low_active=1`
- 本期仅做标记，**不自动冻结**；管理后台商户列表展示「低活跃」标签，便于管理员手动清理
- 商家重新登录后自动清除标记

---

## 三期：平台化与风控

### 三期功能大纲

1. **平台级数据看板**（DAU、车源分布、转化漏斗、全平台会话统计）
2. **防刷与内容安全**（敏感词过滤、图片鉴黄、接口频率限制、消息频率限制）
3. **多城市/区域维度**（商家城市标签、城市筛选预留）
4. **SEO 优化**（Web 端 SSR/预渲染、meta 标签、搜索引擎友好）
5. **高级消息推送**（微信服务号、短信提醒、推送优先级配置）
6. **商家批量操作**（批量上架/下架/删除、Excel 导入车源）
7. **数据导出与报表**（商家导出自有车源、管理员导出平台脱敏数据）
8. **系统架构优化**（消息 WebSocket 升级、Redis 缓存、数据库读写分离）

---

### 1. 平台级数据看板

#### 1.1 核心指标
- 日活用户数（DAU）、新增用户数、累计注册用户数
- 全平台车源总数、在售/已售/下架/违规分布（饼图）
- 全平台会话数、消息总数、人均咨询次数
- 分享链路转化漏斗：分享链接生成 → 打开 → 浏览 ≥3s → 发起咨询 → 收藏

#### 1.2 数据粒度
- 支持按日 / 近 7 日 / 近 30 日 / 自定义区间查看
- 数据每日凌晨由定时任务聚合，避免实时查询大表

#### 1.3 可视化
- 管理后台「数据中心」独立页面，使用图表库展示趋势图、漏斗图、分布图
- 支持数据下钻：点击 DAU 查看活跃用户列表，点击车源数查看对应商家列表

---

### 2. 防刷与内容安全

#### 2.1 敏感词过滤
- 维护敏感词库（文本文件或数据库表），覆盖政治、色情、暴力、广告导流词汇
- 用户/商家发送消息时，后端匹配敏感词库：
  - 命中后消息标记 `is_blocked=1`，不通知对方
  - 用户端提示「消息发送失败，请修改后重试」
  - 管理员后台「留言查看」增加「被拦截消息」筛选，可人工复核

#### 2.2 图片鉴黄
- 接入第三方内容安全服务（如腾讯云天御、阿里云内容安全）或自研模型
- 用户上传车辆图片、微信二维码、申诉图片时，先送审后入库
- 违规图片拒绝上传，前端提示「图片违规，请更换」

#### 2.3 接口频率限制
- 基于 IP + 用户双重限流：
  - 登录 ticket 生成：同一 IP 每分钟 ≤ 10 次
  - 消息发送：同一用户单会话连续发送间隔 ≥ 3 秒
  - 收藏操作：同一用户每分钟 ≤ 30 次
  - 新会话创建：同一用户单日最多发起 10 个新会话（不同商品）
- 超限返回 `429 Too Many Requests`，前端提示「操作过于频繁，请稍后再试」

#### 2.4 反垃圾注册
- 同一设备/同一 IP 短时间内大量注册微信账号时，触发风控标记
- 管理员后台「用户管理」展示可疑账号，支持批量封禁

---

### 3. 多城市/区域维度

#### 3.1 商家城市标签
- 商家入驻时填写「所在城市」（下拉选择，全国地级市列表）
- `shop` 表新增 `city_code`、`city_name` 字段

#### 3.2 城市筛选预留
- 本期**不开放全平台广场**，但在商家主页展示城市标签，为未来公域扩展留口子
- 管理后台支持按城市筛选商家和车源

#### 3.3 未来扩展
- 四期可考虑开放「同城商家」入口（需重新评估多租户隔离策略），本期仅埋点

---

### 4. SEO 优化

#### 4.1 Web 端 SSR/预渲染
- 商品详情页 `/bike/:id` 和商家主页 `/shop/:id` 接入服务端渲染（SSR）或预渲染（Prerender）
- 确保搜索引擎爬虫可抓取车辆标题、品牌、价格、描述等文本内容

#### 4.2 Meta 标签优化
- 商品详情页 title：「{品牌} {车型} {年份} | 售价 {价格} | {商家名称} | 二手摩托车」
- description：截取车辆描述前 100 字 + 里程/排量关键信息
- 图片增加 alt 属性：「{品牌} {车型} 实拍图」

#### 4.3 结构化数据
- 页面注入 JSON-LD 结构化数据（Schema.org 的 Product 类型），包含 name、image、price、brand 等字段
- 提升搜索引擎收录后在搜索结果中的展示丰富度

---

### 5. 高级消息推送

#### 5.1 微信服务号模板消息
- 如平台注册并认证微信服务号，绑定用户 openid
- 用户未订阅小程序消息时，降级推送服务号模板消息（需用户关注服务号）
- 推送场景：商家回复、审核结果通知、违规下架通知

#### 5.2 短信提醒
- 接入短信服务商（如阿里云短信、腾讯云短信）
- 场景：商家回复用户后，如用户 2 小时未读且未开启订阅消息，发送短信提醒（需控制成本，每日限 1 条）
- 商家端：入驻审核通过时发送短信通知

#### 5.3 推送优先级配置
- 管理后台「系统配置」设置推送优先级：
  - 默认：小程序订阅消息 → 服务号 → 短信
  - 可关闭某一层级，避免过度打扰

---

### 6. 商家批量操作

#### 6.1 批量车源操作
- 商家后台车源列表支持复选框，批量操作：上架、下架、删除
- 批量操作异步执行，避免前端长时间等待，完成后推送站内通知

#### 6.2 Excel 导入车源
- 商家后台提供「批量导入」功能，下载标准模板（.xlsx）
- 字段：品牌、车型、排量、年份、里程、价格、是否可过户、描述
- 导入后逐条校验，错误行生成报告，正确行自动创建车源（状态为在售）

---

### 7. 数据导出与报表

#### 7.1 商家导出
- 商家后台「车源管理」支持导出自有车源为 Excel，字段包含车辆信息、状态、发布时间、浏览量（如有）

#### 7.2 管理员导出
- 管理后台支持导出：商家列表、车源列表、用户列表、会话列表
- **用户数据脱敏**：手机号中间 4 位打码，微信 openid 不可导出
- 导出操作记录日志（`admin_export_log` 表），防止数据泄露

---

### 8. 系统架构优化

#### 8.1 消息 WebSocket 升级
- 留言会话从「轮询」升级为 WebSocket 长连接，降低服务器压力和消息延迟
- 实现方案：Django Channels 或独立 WebSocket 服务（Node.js + Redis Pub/Sub）
-  fallback：WebSocket 连接失败时自动降级为轮询

#### 8.2 缓存层引入
- 引入 Redis 缓存：
  - 用户未读数缓存（`unread_count:{user_id}`），TTL 5 分钟
  - 最近访问商家缓存（`recent_shops:{user_id}`），TTL 1 小时
  - 短链映射缓存（`short_link:{code}`），TTL 与短链有效期一致

#### 8.3 数据库读写分离
- 埋点表 `event_log` 数据量过大时，单独部署 ClickHouse 或 Elasticsearch 做日志分析
- MySQL 主库负责写入，从库负责查询，减轻主库压力

---

## 附录：分期边界对照表

| 功能模块 | 一期（MVP · Web 先行） | 二期（增强） | 三期（平台化） |
|---|---|---|---|
| **登录** | Web 扫码、unionid、基础 Token；预留小程序登录 API | 小程序静默登录、Token 踢人、refresh 续期 | — |
| **分享** | shop_id 校验 + HMAC 签名 + 短链（7 天） | 小程序原生分享 | — |
| **浏览** | 详情页、商家主页、Tab、搜索筛选、最近访问 10 条 | 搜索历史、小程序端 | SEO、SSR |
| **车源** | 发布/编辑/下架/已售/删除/排序；丰富字段；展示图 20 + 车况图 10 | 自动压缩、缩略图 | 批量操作、Excel 导入 |
| **收藏** | 增删、失效态 | — | — |
| **留言** | 多轮会话、未读聚合接口、30s 轮询 | 订阅消息、兜底 Banner、归档 | WebSocket、敏感词过滤 |
| **商家后台** | 车源、已售、短链分享、留言、资料 | 数据看板、TOP5 排行、申诉提交 | — |
| **管理后台** | 审核、封禁、车源管控、管理员授权 | 申诉处理、低活跃标记 | 平台看板、数据导出、风控 |
| **消息推送** | 站内红点 | 订阅消息、未授权兜底 | 服务号、短信、优先级配置 |
| **埋点** | — | 核心事件、event_log | 漏斗分析、ClickHouse |
| **安全** | 权限拦截 + 分享验签 | — | 防刷、频率限制、图片鉴黄 |
| **城市** | — | — | 城市标签、预留筛选 |

---

**交付说明**：  
- 一期开发完成后，应能在 **Web** 上支持单个商家完整跑通「入驻→发布→短链分享→客户浏览→咨询→回复」闭环，管理员可审核和管控；已售可见、下架对用户侧隐藏、在售优先按上架时间倒序。  
- 二期开发完成后，补齐**微信小程序端**与商家自运营工具（看板、归档、订阅消息），平台获得基础数据洞察。  
- 三期开发完成后，平台具备**规模化运营能力（SEO、风控、批量工具、高级推送）**。
