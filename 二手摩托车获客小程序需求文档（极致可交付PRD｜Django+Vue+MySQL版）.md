# 二手摩托车获客小程序需求文档（极致可交付PRD｜Django+Vue+MySQL版）

**文档版本**：V1.2 多端微信登录版

**技术栈限定**：后端 Django4.2、前端 Vue、数据库 MySQL

**文档用途**：直接交付后端、前端、UI 开发，无二次需求沟通，可直接拆分支任务、建数据库表、写接口

**版本说明**：
- V1.0：极简多商家入驻、机车发布、留言咨询、微信引流
- V1.1：多租户私域隔离、留言多轮+提醒、收藏删除与失效、列表排序、违规仅管理员恢复、小程序优先
- V1.2（当前）：**方案 A 多端微信登录**（小程序 + Web 扫码联动）、数据多端共享、管理员后台预置与授权

---

## 一、项目整体概述

### 1.1 项目定位

轻量化**多租户**二手摩托车**私域展示获客小程序**，无线上交易、无支付订单、无平台抽佣、无年费付费体系。

核心能力为：商家审核入驻 → 商家私域发布车源 → **分享链接引流** → C 端用户浏览该商家车源 → 多轮留言咨询 → 微信私域转化 → 线下成交。

**与 V1.0 的关键差异**：C 端**不存在全平台车源广场**，用户无法浏览其他商家的商品；仅商家本人、通过分享链接进入的用户、平台管理员可看到对应数据。

### 1.2 技术架构约束（强制落地规则）

- 后端：基于 **Django 4.2** 开发，使用 Django ORM，统一接口返回格式，**多租户数据隔离**由后端强制拦截
- 前端：**优先适配微信小程序（C 端）**；商家后台、管理员后台为 Web 端（Vue），为次要适配目标
- 数据库：**MySQL 8.0+**，核心表含租户/商家维度字段，索引规范
- 部署形态：C 端 = 微信小程序；商家后台 + 管理端 = Web 管理系统

### 1.3 多租户访问模型（核心）

| 角色 | 可见车源范围 |
|---|---|
| 入驻商家 | 仅自身发布的商品（商家后台） |
| C 端用户（无分享链接） | 不可浏览任意商家商品；可访问个人中心、收藏、咨询会话、**最近访问商家** |
| C 端用户（通过分享链接） | 该链接对应商品详情 + **该商家全部可展示商品**；不可见其他商家 |
| 平台管理员 | 全平台所有商家商品（管理后台） |

**分享链接规则**：
- **商品分享链接**：进入对应**商品详情页**（path 携带 `shop_id` + `bike_id`），用户可从详情页进入该**商家店铺页**浏览该商家全部车源
- **店铺分享链接**：直接进入该**商家店铺页**（path 携带 `shop_id`）
- 两种链接均不可跨租户访问其他商家数据
- 用户无分享链接直接进入小程序/Web 时：展示引导页与「最近访问商家」快捷入口（非全平台列表）
- 后端校验：C 端请求商品/商家列表时必须携带合法 `shop_id` 上下文，禁止跨租户查询

### 1.4 核心业务流程（标准闭环）

用户**微信登录**（小程序授权 / Web 扫码） → 商家提交入驻申请 → 管理员审核（通过/驳回） → 审核通过解锁发布权限 → 商家发布机车并**生成分享链接** → 商家将链接分享给潜在客户 → 客户打开链接浏览该商家车源 → 多轮留言咨询（含消息提醒）→ 扫码加微信 → 线下成交

### 1.5 用户登录与多端账号体系（V1.2 新增，强制）

**核心原则**：平台**不提供** C 端/商家前台「账号 + 密码注册」；统一以 **微信身份** 识别用户，**多端数据共享**（同一 `user_id`）。

#### 1.5.1 登录方式（方案 A：微信扫码联动）

| 端 | 登录方式 | 说明 |
|---|---|---|
| 微信小程序 | `wx.login` 静默/授权登录 | 后端 code2session 换取 openid，自动注册或登录 |
| Web C 端 / 商家后台 / 管理端 | **微信扫码登录** | 页面展示登录二维码，用户用微信扫码确认；与小程序**同一微信账号** |
| 管理员 | 微信扫码登录 + `is_staff=1` | **不支持前台自助注册**；须后台预置或现有管理员授权 |

#### 1.5.2 多端联动规则

1. **同一微信 = 同一用户**：小程序与 Web 通过 **unionid**（优先）或 openid 映射到同一条 `user` 记录。
2. **数据共享**：收藏、咨询会话、最近访问商家、入驻状态等均以 `user_id` 存储，**任意端登录后数据一致**。
3. **登录态联动**：用户在 **Web 扫码登录成功** 后，同一微信账号打开 **小程序** 时，`wx.login` 直接识别为已登录用户（无需再次扫码）；反之亦然。
4. **Token 机制**：各端持有独立 access_token（可多端同时在线），后端校验 token 中的 `user_id` 一致即可。

#### 1.5.3 Web 扫码登录流程

```
Web 端请求生成登录二维码 ticket
  → 展示二维码（含 ticket）
  → 用户微信扫码（小程序确认页 / 微信开放平台扫码页）
  → 后端写入 ticket 与 user 绑定，状态变为已确认
  → Web 端轮询 ticket 状态，获取 token + 用户信息
  → 跳转回原页面
```

#### 1.5.4 注册说明

- **C 端用户**：**无需注册**；首次微信登录自动创建账号。
- **商家**：微信登录后，在「个人中心」提交**入驻申请**（非账号注册）。
- **管理员**：
  - 系统部署时 **预置首个超级管理员**（Django 命令或 SQL，绑定微信 unionid / 手动授予 `is_staff`）。
  - 已有管理员可在管理端 **「管理员管理」** 将指定微信用户设为/取消管理员。
  - **禁止**前台注册管理员。

#### 1.5.5 未登录访问策略

| 页面类型 | 未登录是否可访问 |
|---|---|
| 首页引导、商品详情（分享链）、商家主页（分享链） | 可浏览（收藏/留言需登录） |
| 我的收藏、我的咨询、个人中心、商家入驻 | **需登录**，跳转登录页 |
| 商家后台、管理端 | **需登录** + 对应角色权限 |

### 1.6 端适配优先级

1. **P0 — 微信小程序 C 端**：分享进入、商品详情、商家主页、收藏、多轮咨询、消息提醒、二维码引流
2. **P1 — Web 商家后台**：车源 CRUD、留言回复、资料/二维码管理
3. **P2 — Web 管理端**：审核、封禁、全平台车源/留言管控
4. **P3 — Web C 端**：与小程序能力对齐；**微信扫码登录**，数据与小程序共享

---

## 二、用户角色与权限体系（精准隔离）

系统包含三类角色，**租户级数据完全隔离**，权限严格分级。

### 2.1 普通 C 端用户

- 通过**分享链接**访问指定商家域内的商品详情、商家主页
- 可收藏/删除收藏、发起并参与多轮留言咨询
- 可查看「我的收藏」「我的咨询」「最近访问商家」
- **不可**：浏览其他商家商品、发布/编辑车源、跨租户访问

### 2.2 入驻商家（多租户商户）

- 审核通过后**永久有效**，无时效、无付费门槛
- 仅可管理**自身租户**的车源、留言会话、资料、分享链接
- 不可查看/操作其他商家任何数据
- **不可恢复**「违规下架(status=4)」车辆，仅可联系管理员

### 2.3 平台管理员

- 审核商家、封禁/解封商户
- 查看并管控**全平台**车源与留言
- **唯一**可将「违规下架(status=4)」恢复为在售
- 不可篡改商家私有资料（微信二维码等）
- **登录方式**：Web 端微信扫码；账号须 `is_staff=1`
- **授权方式**：首个管理员由后台预置；之后可在「管理员管理」授予/撤销其他用户的 `is_staff`，**不支持前台注册**

---

## 三、全局状态枚举（后端数据库固定枚举）

所有状态后端统一写死，前端仅做展示与交互，禁止前端自定义状态。

### 3.1 商家入驻状态（shop_status）

- 0：普通用户（未申请入驻）
- 1：待审核（已提交申请）
- 2：审核通过（正式商家，拥有全部权限）
- 3：审核驳回（可重新提交）
- 4：违规封禁（永久冻结商户权限）

### 3.2 机车车辆状态（bike_status）

- 1：在售（该商家域内正常展示，列表优先）
- 2：已售（展示已售标签，排在在售之后）
- 3：下架（商家手动下架，该商家域内前台隐藏，商家后台可见；商家可自行重新上架）
- 4：违规下架（管理员强制下架，**仅管理员可恢复**，商家不可操作恢复）

### 3.3 商品删除标记（is_deleted）

- 0：正常
- 1：已逻辑删除（商家删除）；C 端收藏列表展示「商家已删除」，**不可进入详情**

### 3.4 留言会话状态（thread_status）

会话维度状态（非单条消息）：

- 1：未读（对方有新消息，当前角色未读）
- 2：已读未回复
- 3：已回复（商家已回复，用户未再发新消息）

> 单条消息另存 `message_item` 表，支持多轮；会话状态由最新消息与已读标记计算，后端为准。

### 3.5 消息发送方（sender_type）

- 1：C 端用户
- 2：商家
- 3：系统通知（可选，用于状态变更提示）

---

## 四、核心功能详细 PRD

### 4.1 商家入驻模块

（与 V1.0 一致，略）

**页面入口**：小程序个人中心 - 商家入驻  
**权限控制**：仅 `shop_status=0` 可见入口

字段与交互规则同 V1.0，此处不重复。

---

### 4.2 商家机车发布/编辑模块（核心）

（字段定义与 V1.0 一致）

**新增规则**：

- 每辆车发布成功后生成**可分享链接/小程序码**，商家可在车源列表一键复制分享
- 记录 `published_at`（上架时间），用于列表排序；编辑不影响排序时保留首次上架时间
- 商家删除车辆为**逻辑删除**（`is_deleted=1`），前台不可见；收藏侧展示失效态
- 商家对 `bike_status=4` 的车辆：**不可**「重新上架」，操作区仅展示「联系平台」或隐藏恢复按钮

**列表排序（商家后台 + 商家域 C 端主页统一）**：

1. 优先展示 `bike_status=1`（在售）
2. 同状态内按 `published_at` **倒序**
3. `bike_status=2`（已售）排在在售之后，同样按 `published_at` 倒序
4. 支持 Tab/筛选：**在售（默认）** / **已售** / **全部**（商家后台另含「已下架」）

---

### 4.3 商家私域引流模块（微信二维码）

（与 V1.0 一致）

展示位置：商家主页顶部、每台机车详情页底部固定区。

---

### 4.4 用户留言咨询模块（多轮对话）

**功能逻辑**：以「会话 thread」为单元，**归属单一商家 + 单一用户 + 单一商品**，跨商家完全隔离。

**入口**：
- 商品详情页「在线留言咨询」→ 进入/创建该用户与该商品的会话页
- 个人中心「我的咨询」→ 会话列表

**交互（类微信对话）**：
- 消息气泡左右分布（用户右/商家左，或按角色区分）
- 支持**多轮**收发，单条内容 ≤500 字
- 首条消息可携带联系电话（选填），后续消息仅文本
- 会话永久留存，不可手动删除（纠纷溯源）
- 进入会话页自动标记已读，更新 `thread_status`

**消息提醒**：
- **小程序内**：TabBar/入口红点 + 会话列表未读数 + 商家后台留言菜单角标
- **微信订阅消息**（用户授权后）：对方回复时推送模板消息，跳转对应会话页
- **两者均需实现**（产品已确认）
- 后端提供未读计数接口；前端轮询或 WebSocket（一期可用轮询）

**权限**：
- 用户：仅自己的会话
- 商家：仅本店会话
- 管理员：全平台只读，不可代回复

---

### 4.5 C 端用户浏览功能（多租户私域）

**取消**：全平台「车源广场」、跨商家筛选列表。

**保留/新增**：

| 页面 | 说明 |
|---|---|
| 小程序首页 | 引导文案 + **最近访问商家**（本地/服务端记录）+ 快捷入口（收藏、咨询） |
| 商品详情 | 仅通过分享链接或从同商家域内跳转进入 |
| 商家主页 | 展示**该商家**在售/已售车源，默认在售 Tab，支持筛「已售」 |
| 我的收藏 | 列表展示；支持**删除收藏**；商品被商家逻辑删除后显示「商家已删除」，禁用点击 |
| 我的咨询 | 多轮会话列表，未读提醒 |

**商家域内筛选**（仅在商家主页）：价格区间、排量、年份、是否可过户（可选，与 V1.0 相同条件，范围限定在当前 shop_id）

---

### 4.6 商家后台中心

功能清单：
- 车源管理（新增/编辑/下架/删除/**分享链接**）
- 留言会话管理（多轮回复、未读提醒）
- 个人资料编辑、二维码管理、入驻状态查看

**车源列表**：默认在售优先 + `published_at` 倒序；筛选含已售/已下架；`status=4` 无「重新上架」

---

### 4.7 管理员后台核心能力

- 商家审核、商户封禁/解封
- **全平台**车源查看与管控
- **违规下架**与**恢复**：仅管理员可将 `bike_status=4` 恢复为 `1`（在售）
- 全平台留言会话只读查看
- **管理员管理（V1.2）**：查看平台用户列表、授予/撤销 `is_staff`（不可删除预置超级管理员）

### 4.8 登录模块（V1.2）

**Web 登录页**（`/login`）：
- 展示微信扫码登录二维码
- 支持扫码状态：等待扫码 / 已扫码待确认 / 登录成功 / 已过期
- 登录成功后写 token，跳转 `redirect` 原页面
- 底部说明：「使用微信扫码登录，与小程序账号互通，无需注册」

**小程序登录**：
- 启动时 `wx.login`，无登录页；失败时提示重试
- 登录成功后拉取 `/auth/me` 同步用户信息

**商家/管理端 Web**：
- 未登录统一跳转 `/login?redirect=...`
- 商家后台额外校验 `shop_status=2`；管理端额外校验 `is_staff=1`

---

## 五、核心业务强制规则

1. **永久权限**：商家审核通过后永久有效，无租约/时效/续费逻辑
2. **多租户隔离**：C 端 API 必须校验 `shop_id` 租户上下文；商家仅 CRUD 本租户数据；管理员全量
3. **分享私域**：获客主路径为商家分享链接，非平台公域流量
4. **违规恢复**：`bike_status=4` **仅管理员**可恢复；商家手动下架(3) 由商家自行上架
5. **合规引流**：仅展示二维码，无诱导分享、无强制加微
6. **无交易**：无订单、支付、流水、售后
7. **逻辑删除**：商品/业务数据软删；收藏对已删商品展示失效态
8. **端优先级**：C 端小程序 > Web 商家/管理端 > Web C 端
9. **多端登录**：仅微信登录，无 C 端密码注册；Web 扫码与小程序账号互通，业务数据按 `user_id` 共享
10. **管理员**：仅后台预置 + 管理员授权，禁止前台注册

---

## 六、技术适配规范

### 6.1 后端 Django

- DRF 统一 `{ code, msg, data }`
- **Auth API**：`/auth/wx-mini/login`、`/auth/wx-scan/ticket`、`/auth/wx-scan/poll`、`/auth/me`、`/auth/logout`
- **TenantPermission**：基于 `shop_id` + 角色拦截
- 分享链接校验：短链或 signed token 绑定 shop_id
- 留言：`message_thread` + `message_item` 两表
- 收藏：`favorite` 表，关联 `bike_id`；返回时附带 `is_deleted` 快照
- 未读数：按角色聚合接口
- 管理员授权：`POST /admin/users/{id}/grant-staff`、`POST /admin/users/{id}/revoke-staff`（仅 is_staff 可操作）

### 6.2 前端 Vue

- **小程序优先**：C 端页面、分享、订阅消息、长按识别二维码
- **Web 登录页**：微信扫码组件 + ticket 轮询
- Web C 端与小程序信息架构对齐，无全平台列表
- 表单双校验，权限以后端为准
- 收藏删除调用 DELETE；失效商品禁用路由跳转

### 6.3 MySQL 核心表

| 表名 | 说明 |
|---|---|
| user | 用户（含 mp_openid、web_openid、unionid、is_staff 授权字段） |
| auth_login_ticket | Web 微信扫码登录票据 |
| shop / shop_application | 商家与入驻申请 |
| bike | 机车，`shop_id`、`published_at`、`is_deleted` |
| message_thread | 留言会话 |
| message_item | 会话消息明细 |
| favorite | 用户收藏 |
| user_shop_visit | 最近访问商家 |

索引：`shop_id`、`bike_status`、`published_at`、`user_id`、`thread_id`

---

## 七、前端页面清单（V1.2）

### 7.0 公共页面

| 页面 | 路由 | 要点 |
|---|---|---|
| 微信扫码登录 | `/login` | Web 端统一登录入口；ticket 轮询；与小程序账号互通 |

### 7.1 小程序 C 端（P0）

| 页面 | 路由/Path | 要点 |
|---|---|---|
| 首页 | `/pages/index` | 引导 + 最近访问商家，无全平台列表 |
| 商品详情 | `/pages/bike/detail` | 分享入口，留言、收藏、二维码 |
| 商家主页 | `/pages/shop/home` | 租户内列表，在售优先，筛已售 |
| 我的收藏 | `/pages/user/favorites` | 删除收藏，失效态 |
| 咨询会话列表 | `/pages/user/messages` | 未读角标 |
| 咨询会话详情 | `/pages/user/chat` | 多轮对话 |
| 个人中心 | `/pages/user/profile` | 入驻入口；未登录引导授权 |

### 7.2 Web 商家后台（P1）

> 未登录跳转 `/login`；须 `shop_status=2`

| 页面 | 路由 | 要点 |
|---|---|---|
| 车源列表 | `/shop/bikes` | 排序、分享、违规不可恢复 |
| 车源表单 | `/shop/bikes/new|edit` | 同 V1.0 字段 |
| 留言管理 | `/shop/messages` | 多轮会话 UI |
| 商家资料 | `/shop/profile` | 资料 + 二维码 |

### 7.3 Web 管理端（P2）

> 未登录跳转 `/login`；须 `is_staff=1`

| 页面 | 路由 | 要点 |
|---|---|---|
| 商家审核 | `/admin/audit` | 同 V1.0 |
| 商户管理 | `/admin/shops` | 同 V1.0 |
| 车源管控 | `/admin/bikes` | 全平台；违规恢复 |
| 留言查看 | `/admin/messages` | 只读 |
| **管理员管理** | `/admin/users` | 授予/撤销 is_staff；不可撤销首个超管 |

### 7.4 Web C 端（P3）

与小程序页面能力对齐；**须微信扫码登录后**访问需登录页面：
- `/login` 登录
- `/` 首页（引导页，未登录可浏览）
- `/bike/:id` 详情（需 shop 上下文，未登录可浏览）
- `/shop/:id` 商家主页
- `/favorites` 收藏
- `/messages`、`/messages/:threadId` 咨询
- `/profile` 个人中心

---

## 八、一期开发排期清单（V1.2 更新）

1. **微信登录体系**：小程序 wx.login + Web 扫码 ticket + 多端 user 统一（unionid）
2. 小程序框架、分享 link 参数解析、租户上下文
3. 商家入驻 + 管理员审核
4. **多租户权限拦截** + 分享链接校验
5. 机车发布/编辑/下架/删除/分享 + `published_at` 排序
6. 媒体上传与详情展示
7. 二维码引流
8. **多轮留言** + 未读计数 + 订阅消息提醒
9. C 端商家域浏览、收藏（含删除与失效态）
10. 管理员全平台管控 + 违规恢复 + **管理员授权**
11. 小程序 + Web 多端适配 + 合规兜底

---

## 九、待产品确认项（实现前可微调）

1. ~~无分享链接进入首页内容~~ → 已确认
2. ~~登录方案~~ → 已确认：方案 A，微信扫码 + 小程序联动，数据共享
3. **订阅消息模板**：需在微信公众平台配置具体模板 ID 与文案
4. **微信开放平台**：Web 扫码登录需绑定开放平台以获取 unionid（与小程序同主体）

---

## 十、版本变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| V1.0 | - | 初版 PRD |
| V1.1 | - | 多租户私域、留言多轮+提醒、收藏删除与失效、列表排序、违规仅管理员恢复、小程序优先 |
| V1.2 | - | 方案 A 多端微信登录、Web 扫码与小程序联动、管理员预置与授权、登录页与管理员管理页 |

> （注：部分内容可能由 AI 生成）
